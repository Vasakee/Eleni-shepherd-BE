<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Event Registration Confirmation</title>
    <style>
      /* Reset styles */
      body,
      table,
      td,
      a {
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
      }
      table,
      td {
        mso-table-lspace: 0pt;
        mso-table-rspace: 0pt;
      }
      img {
        -ms-interpolation-mode: bicubic;
      }

      /* Base styles */
      body {
        font-family: 'Poppins', Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #010d1f;
        color: #ffffff;
      }
      table {
        border-collapse: collapse;
        border-spacing: 0;
      }
      td {
        padding: 0;
      }

      /* Main styles */
      .wrapper {
        max-width: 600px;
        margin: 40px auto;
        background-color: #ffffff;
        color: #333;
        box-shadow: 0 12px 24px rgba(0, 0, 0, 0.08);
      }
      .header {
        background-color: #004ba8;
        padding: 48px 20px;
        text-align: center;
        border-top-left-radius: 20px;
        border-top-right-radius: 20px;
      }
      .header h1 {
        margin: 0;
        font-size: 36px;
        font-weight: 700;
        color: #ffffff;
        text-align: center;
      }
      .header p {
        margin: 0;
        font-size: 18px;
      }

      .logo-container {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin: 20px 0;
      }
      .logo-container img {
        max-height: 100px;
        width: 150px;
        object-fit: cover;
      }

      .content {
        padding: 20px 40px;
        margin-bottom: 10px;
      }
      .event-details {
        display: block;
        gap: 16px;
        margin-bottom: 24px;
        margin-top: 24px;
      }
      .event-info {
        flex: 1;
      }
      .event-info h2 {
        font-size: 24px;
        font-weight: 600;
        text-align: center;
      }
      .divider {
        background-color: #004ba8;
        margin-bottom: 16px;
        margin-top: 16px;
        padding: 1px;
        border-radius: 50%;
      }
      .event-info ul {
        list-style-type: none;
        padding: 0;
        margin: 0;
      }
      .event-info li {
        font-size: 16px;
        margin-bottom: 8px;
      }
      .qr-code-container {
        text-align: center;
      }
      .qr-code-container img {
        max-width: 200px;
        border: 1px solid #ddd;
        padding: 10px;
        border-radius: 8px;
      }

      .cta-container {
        text-align: center;
        margin: 20px 0;
      }
      .cta {
        background-color: #004ba8;
        color: #fff;
        display: inline-block;
        padding: 15px 30px;
        text-decoration: none;
        border-radius: 8px;
        font-size: 18px;
        font-weight: 600;
        text-align: center;
        margin: 8px;
      }
      .footer {
        background-color: #004ba8;
        color: #fff;
        padding: 24px;
        border-bottom-left-radius: 20px;
        border-bottom-right-radius: 20px;
        font-size: 14px;
        text-align: center;
      }
      .footer a {
        color: #fff;
        text-decoration: none;
      }

      
    </style>
  </head>
  <body>
    <div class="wrapper">
      <!-- Header with Logos -->
      <div class="header">
        <div class="logo-container">
          <img src="<%= blueticketLogo %>" alt="Blueticket Logo" />
          <img src="<%= eventLogo %>" alt="Event Logo" />
        </div>
        <h1>
          <%= eventName %> <br />
          Event Ticket
        </h1>
      </div>

      <!-- Content -->
      <div class="content">
        <p>Hello <%= attendeeName %>,</p>
        <p>
          Thank you for registering for <strong><%= eventName %></strong>! We’re
          excited to have you join us. Below is your registered event details
          and information.
        </p>

        <hr class="divider" />
        <!-- Ticket Info -->
        <p>
          <strong>Ticket ID</strong> <br />
          <%= ticketId %>
        </p>
        <p>
          <strong>Registered Email</strong> <br />
          <%= email %>
        </p>
        <!-- Event Details and QR Code -->
        <div class="event-details">
          <div class="qr-code-container">
            <img src="<%= qrCode %>" alt="QR Code" />
          </div>
          <div class="event-info">
            <h2>Event Details</h2>
            <ul>
              <li><strong>Event:</strong> <%= eventName %></li>
              <li><strong>Date:</strong> <%= eventDate %></li>
              <li><strong>Time:</strong> <%= eventTime %></li>
              <li><strong>Venue:</strong> <%= eventVenue %></li>
            </ul>
          </div>
        </div>

        <hr class="divider" />
        <!-- Important Reminders -->
        <h2  style="text-align: center;">Important Reminders</h2>
        <ul style="list-style-type:  '✓ ';">
          <li>Bring a valid ID to verify your registration.</li>
          <li>Save this email to ensure easy access to your ticket.</li>
          <li>We recommend arriving 30 minutes early to complete check-in.</li>
        </ul>

        <!-- Call to Action Buttons -->
        <div class="cta-container">
          <a href="<%= rsvpLink %>" class="cta">RSVP Now</a>
        </div>
      </div>

      <!-- Footer -->
      <div class="footer">
        <p>Looking forward to seeing you at the event!</p>
        <p>&copy; 2024 Blueticket Events</p>
      </div>
    </div>
  </body>
</html>
